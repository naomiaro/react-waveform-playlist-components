import React from 'react';
import { StyledTimeScale } from '../src/components/TimeScale';

export default {
  title: 'Styled Timescale',
  component: StyledTimeScale,
};

export const Default = () => <StyledTimeScale duration={30} />;
