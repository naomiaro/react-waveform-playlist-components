import React from 'react';
import { SmartScale } from '../src/components/TimeScale';

export default {
  title: 'Smartscale',
  component: SmartScale,
};

export const Default = () => <SmartScale />;
